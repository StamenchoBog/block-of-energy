{
  "name": "hash_and_store_to_ledger",
  "version": "1.0.0",
  "description": "Azure Function to store data in Hyperledger Fabric.",
  "scripts": {
    "build": "tsc && npm run copy-files",
    "copy-files": "cp -r src/crypto dist/src/",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "4.7.2",
    "@grpc/grpc-js": "^1.9.0",
    "@hyperledger/fabric-gateway": "^1.4.0"
  },
  "devDependencies": {
    "@types/google-protobuf": "^3.15.12",
    "@types/node": "18.x",
    "azure-functions-core-tools": "^4.x",
    "rimraf": "^5.0.0",
    "typescript": "^4.0.0"
  },
  "main": "dist/src/functions/hashAndStoreToLedger.js"
}